---
title: "System Design Basics"
date: "Jan 2026"
description: "A beginner-friendly guide to system design interviews"
---

## What is System Design?

System design is about building scalable, reliable systems.

### Key concepts
- Scalability
- Availability
- Latency
- Trade-offs

## Example: Simple Load Balancer

Here's a basic implementation of a round-robin load balancer in Python:

```python
class LoadBalancer:
    def __init__(self, servers):
        self.servers = servers
        self.current = 0
    
    def get_next_server(self):
        server = self.servers[self.current]
        self.current = (self.current + 1) % len(self.servers)
        return server

# Usage
lb = LoadBalancer(['server1', 'server2', 'server3'])
print(lb.get_next_server())  # server1
print(lb.get_next_server())  # server2
```

### Node.js Implementation

Here's the same concept in JavaScript:

```javascript
class LoadBalancer {
    constructor(servers) {
        this.servers = servers;
        this.current = 0;
    }
    
    getNextServer() {
        const server = this.servers[this.current];
        this.current = (this.current + 1) % this.servers.length;
        return server;
    }
}

// Usage
const lb = new LoadBalancer(['server1', 'server2', 'server3']);
console.log(lb.getNextServer()); // server1
console.log(lb.getNextServer()); // server2
```

## Configuration Example

Here's a sample configuration file:

```yaml
load_balancer:
  algorithm: round-robin
  servers:
    - host: 192.168.1.10
      port: 8080
    - host: 192.168.1.11
      port: 8080
    - host: 192.168.1.12
      port: 8080
  health_check:
    interval: 30s
    timeout: 5s
```
